@model HashSet<AppIntegrador.Models.Objetivo>

@{
    ViewBag.Title = "Objetivos";
}

<h2>Objetivos</h2>

@if (Model.Count() != 0)
{
    if((int) ViewContext.RouteData.Values["user_id"] == 0)
    {

    }
    <p>yay</p>
}
else
{
    <div>
        <h3 class="centeredText">Presione el botón "Crear Nuevo" para agregar un nuevo Plan</h3>
    </div>
}
<div style="float:right;">
    @Html.ActionLink("Crear Nuevo", "CrearAsociado", "Objetivos", new { user_id = Request.Params["user_id"], codigo = Request.Params["codigo"] }, new { @class = "btn btn-primary btn-sm" })
</div>

<div style="float:left">
    @Html.ActionLink("Regresar", "Index")
</div>

